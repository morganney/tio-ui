const BRANCH_NAME = 'containerSecurity';
const STEM_NAME = 'policies';
const REDUX_FORM_NEW_POLICY = `${BRANCH_NAME}.${STEM_NAME}.newPolicyForm`;
const REDUX_FORM_EDIT_POLICY = `${BRANCH_NAME}.${STEM_NAME}.editPolicyForm`;

export {
    BRANCH_NAME,
    STEM_NAME,
    REDUX_FORM_NEW_POLICY,
    REDUX_FORM_EDIT_POLICY
};
