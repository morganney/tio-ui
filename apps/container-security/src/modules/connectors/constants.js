const BRANCH_NAME = 'containerSecurity';
const STEM_NAME = 'connectors';
const CONNECTOR_REDUX_FORM = `${BRANCH_NAME}.${STEM_NAME}.connectorForm`;

export {
    BRANCH_NAME,
    STEM_NAME,
    CONNECTOR_REDUX_FORM
};
