import * as containers from './containers';
import * as presentations from './presentations';

const {
    LandingContainer: LandingComponent
} = containers;

export {
    LandingComponent,
    containers,
    presentations
};
