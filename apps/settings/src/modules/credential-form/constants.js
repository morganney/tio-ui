const BRANCH_NAME = 'settings';
const STEM_NAME = 'credentialForm';
const SETTINGS_REDUX_FORM = `${BRANCH_NAME}.${STEM_NAME}.settingsForm`;

export {
    BRANCH_NAME,
    STEM_NAME,
    SETTINGS_REDUX_FORM
};
